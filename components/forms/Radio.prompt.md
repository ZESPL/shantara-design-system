Single choice among a small visible set. Group with a `role="radiogroup"` wrapper and a shared `name`.

```jsx
<Radio name="nights" label="7 nights" description="the clinical minimum" card defaultChecked />
<Radio name="nights" label="14 nights" description="recommended for detox" card />
```
